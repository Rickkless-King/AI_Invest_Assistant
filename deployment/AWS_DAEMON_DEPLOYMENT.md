# AWS 后台守护进程部署指南

## 📋 目录
- [问题说明](#问题说明)
- [解决方案](#解决方案)
- [部署步骤](#部署步骤)
- [验证与监控](#验证与监控)
- [故障排除](#故障排除)

---

## 问题说明

### 之前的运行方式
```
用户打开网页 → Streamlit 启动 → 后台线程监控（每60秒）
用户关闭网页 → Streamlit 停止 → 监控停止 ❌
用户再次打开 → 显示"同步最新 X 小时数据" ⚠️
```

**问题**：虽然部署在 AWS 上，但数据同步依赖于用户是否打开网页，无法实现真正的 24/7 自动交易。

### 新的运行方式
```
AWS 服务器启动 → 守护进程自动启动 → 24/7 持续监控 ✅
Streamlit 网页   → 仅用于查看状态和管理 ✅
```

**改进**：独立的后台守护进程，与 Streamlit 解耦，实现真正的全天候自动交易。

---

## 解决方案

### 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                     AWS EC2 服务器                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  守护进程 (arena_daemon.py)                      │  │
│  │  ✅ 开机自动启动                                 │  │
│  │  ✅ 每60秒同步数据并执行交易                     │  │
│  │  ✅ 每4小时优化 Agent 参数                       │  │
│  │  ✅ 崩溃自动重启                                 │  │
│  │  ✅ 记录详细日志                                 │  │
│  └──────────────────────────────────────────────────┘  │
│                          ↕                               │
│  ┌──────────────────────────────────────────────────┐  │
│  │  SQLite 数据库                                   │  │
│  │  - historical_klines.db (K线数据)                │  │
│  │  - arena_state.db (竞技场状态)                   │  │
│  └──────────────────────────────────────────────────┘  │
│                          ↕                               │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Streamlit 网页 (streamlit_app.py)               │  │
│  │  ✅ 查看实时状态                                 │  │
│  │  ✅ 管理策略参数                                 │  │
│  │  ✅ 查看交易历史                                 │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
                          ↕
                   OKX API (实时行情)
```

---

## 部署步骤

### 步骤 1: 连接到 AWS 服务器

使用 PuTTY 或 SSH 连接到您的 AWS EC2 实例：

```bash
ssh -i your-key.pem ubuntu@your-ec2-ip
```

### 步骤 2: 进入项目目录

```bash
cd ~/AI_Invest_Assistant
```

### 步骤 3: 创建日志目录

```bash
mkdir -p logs
```

### 步骤 4: 测试守护进程（可选但推荐）

在正式部署前，先手动测试守护进程是否正常运行：

```bash
# 激活虚拟环境
source venv/bin/activate

# 运行守护进程（前台模式）
python3 backend/arena_daemon.py
```

**预期输出**：
```
============================================================
🚀 策略竞技场守护进程启动
============================================================
✅ 加载已有竞技场状态 (上次活跃: 2026-01-18 08:30:15)
🔄 检测并同步离线数据...
📊 已同步离线 0.5 小时的数据
✅ 竞技场初始化成功
🔍 开始监控循环...
```

如果一切正常，按 `Ctrl+C` 停止测试。

### 步骤 5: 安装 systemd 服务

```bash
# 1. 复制服务文件到系统目录
sudo cp deployment/arena-daemon.service /etc/systemd/system/

# 2. 修改服务文件中的路径（如果您的路径不是 /home/ubuntu/AI_Invest_Assistant）
sudo nano /etc/systemd/system/arena-daemon.service
# 修改以下行：
#   WorkingDirectory=/home/ubuntu/AI_Invest_Assistant
#   ExecStart=/home/ubuntu/AI_Invest_Assistant/venv/bin/python3 ...
#   StandardOutput/StandardError 路径

# 3. 重新加载 systemd
sudo systemctl daemon-reload

# 4. 启用服务（开机自动启动）
sudo systemctl enable arena-daemon.service

# 5. 启动服务
sudo systemctl start arena-daemon.service
```

### 步骤 6: 验证服务状态

```bash
# 查看服务状态
sudo systemctl status arena-daemon.service
```

**预期输出**：
```
● arena-daemon.service - AI Invest Assistant - Strategy Arena Daemon
   Loaded: loaded (/etc/systemd/system/arena-daemon.service; enabled)
   Active: active (running) since Sat 2026-01-18 10:00:00 UTC; 5s ago
 Main PID: 12345 (python3)
   Status: "Running..."
    Tasks: 2
   Memory: 50.0M
      CPU: 2s
   CGroup: /system.slice/arena-daemon.service
           └─12345 /home/ubuntu/AI_Invest_Assistant/venv/bin/python3 ...
```

**关键标志**：
- `Loaded: loaded` - 服务已加载 ✅
- `enabled` - 开机自动启动已启用 ✅
- `Active: active (running)` - 服务正在运行 ✅

---

## 验证与监控

### 查看实时日志

```bash
# 查看应用日志（守护进程主日志）
tail -f logs/arena_daemon.log

# 查看系统标准输出
tail -f logs/daemon_stdout.log

# 查看系统错误输出
tail -f logs/daemon_stderr.log

# 查看 systemd 服务日志
sudo journalctl -u arena-daemon.service -f
```

### 常用管理命令

```bash
# 启动服务
sudo systemctl start arena-daemon.service

# 停止服务
sudo systemctl stop arena-daemon.service

# 重启服务
sudo systemctl restart arena-daemon.service

# 查看服务状态
sudo systemctl status arena-daemon.service

# 禁用开机自动启动
sudo systemctl disable arena-daemon.service

# 启用开机自动启动
sudo systemctl enable arena-daemon.service
```

### 监控交易活动

守护进程会每60秒检查一次，日志会显示：

```
============================================================
🔄 第 1 次检查 (2026-01-18 10:01:00)
============================================================
✅ 执行了 2 笔交易:
   RSI策略 | BUY | 价格: $42500.00 | 数量: 0.047059 | 金额: $2000.00
   MACD策略 | SELL | 价格: $42500.00 | 数量: 0.047059 | 金额: $2000.00
💾 已保存竞技场状态

📊 当前竞技场状态:
   总资产: $10250.00 USDT
   总收益率: 2.50%
   RSI策略: 收益 +3.50% | 持仓 0.047059 BTC
   MACD策略: 收益 +1.20% | 持仓 0.000000 BTC
   ...

💤 休眠 60 秒...
```

### 在 Streamlit 网页查看

现在打开 Streamlit 网页时：

1. **不会再显示"同步最新 X 小时数据"** - 因为守护进程一直在同步
2. **可以实时查看策略状态** - 数据由守护进程持续更新
3. **可以调整策略参数** - 修改后会保存到数据库，守护进程下次检查时生效

---

## 工作流程对比

### 之前的工作流程 ❌

```
08:00 - 用户打开网页
      → 显示"同步最新 10.1 小时数据"
      → 一次性回测离线期间的交易
      → 启动竞技场
      → 后台线程开始监控

12:00 - 用户关闭网页
      → Streamlit 停止
      → 后台线程停止
      → 数据同步停止

18:00 - 用户再次打开网页
      → 显示"同步最新 6 小时数据"
      → 再次回测
      → ...
```

**问题**：
- 错过实时交易机会 ❌
- 只能模拟历史交易 ❌
- Agent 无法及时优化 ❌

### 现在的工作流程 ✅

```
系统启动 - 守护进程自动启动
         ↓
00:00 - 自动检查 → 执行交易 → 保存状态 → 休眠60秒
01:00 - 自动检查 → 执行交易 → 保存状态 → 休眠60秒
02:00 - 自动检查 → 执行交易 → 保存状态 → 休眠60秒
...
04:00 - 自动检查 → Agent 优化参数 → 保存状态
...
24:00 - 持续运行...

用户随时打开网页 → 查看实时最新状态 → 无需等待同步 ✅
```

**优势**：
- 24/7 实时交易 ✅
- Agent 及时响应市场变化 ✅
- 不错过任何交易机会 ✅

---

## 故障排除

### 问题 1: 服务启动失败

**检查日志**：
```bash
sudo journalctl -u arena-daemon.service -n 50
```

**常见原因**：
- Python 路径错误 → 检查 `ExecStart` 路径
- 虚拟环境未激活 → 确保使用 `venv/bin/python3`
- 权限问题 → 检查 `User` 和 `Group`

### 问题 2: 守护进程频繁重启

**检查日志**：
```bash
tail -f logs/arena_daemon.log
```

**常见原因**：
- OKX API 密钥错误 → 检查 `.env` 文件
- 网络连接问题 → 检查 AWS 安全组和网络配置
- 数据库权限问题 → 确保 `data/` 目录可写

### 问题 3: 竞技场未分配资金

**错误日志**：
```
⚠️ 竞技场未分配资金，请在网页界面启动竞技场
```

**解决方法**：
1. 打开 Streamlit 网页
2. 点击"🚀 启动竞技场"按钮
3. 等待初始化完成
4. 守护进程会自动检测并开始监控

### 问题 4: 日志文件过大

**清理日志**：
```bash
# 备份旧日志
mv logs/arena_daemon.log logs/arena_daemon.log.old

# 重启服务（生成新日志）
sudo systemctl restart arena-daemon.service
```

**设置日志轮转**（推荐）：
```bash
sudo nano /etc/logrotate.d/arena-daemon
```

内容：
```
/home/ubuntu/AI_Invest_Assistant/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
}
```

---

## 性能优化建议

### 1. 调整检查间隔

编辑守护进程代码 `backend/arena_daemon.py`:

```python
config = ArenaConfig(
    symbol="BTC-USDT",
    timeframe="1H",
    check_interval=60,  # 改为 30 秒或 120 秒
    is_demo=True
)
```

### 2. 调整 Agent 优化频率

编辑守护进程代码：

```python
# 当前是每4小时优化一次
if hours_since_optimize >= 4:  # 改为 2 或 6
    ...
```

### 3. 监控系统资源

```bash
# 查看 CPU 和内存使用
top -p $(pgrep -f arena_daemon)

# 查看磁盘使用
df -h
du -sh data/ logs/
```

---

## 安全建议

1. **保护 API 密钥**：
   ```bash
   chmod 600 .env
   ```

2. **定期备份数据库**：
   ```bash
   # 创建备份脚本
   cat > backup.sh << 'EOF'
   #!/bin/bash
   DATE=$(date +%Y%m%d_%H%M%S)
   tar -czf backups/backup_$DATE.tar.gz data/*.db
   find backups/ -mtime +7 -delete
   EOF

   chmod +x backup.sh

   # 添加到 crontab（每天凌晨2点备份）
   crontab -e
   # 添加: 0 2 * * * /home/ubuntu/AI_Invest_Assistant/backup.sh
   ```

3. **监控异常活动**：
   ```bash
   # 设置告警（交易金额过大时）
   # 可以在守护进程中添加邮件或 Telegram 通知
   ```

---

## 总结

✅ **守护进程已部署** - 24/7 自动运行
✅ **开机自动启动** - 无需手动干预
✅ **崩溃自动重启** - 高可用性
✅ **详细日志记录** - 易于监控和调试
✅ **与 Streamlit 解耦** - 互不干扰

现在，您的策略竞技场将真正实现**全天候自动交易**，不再受限于网页是否打开！🚀
